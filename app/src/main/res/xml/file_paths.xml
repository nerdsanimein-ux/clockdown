<?xml version="1.0" encoding="utf-8"?>
<!-- Only the folder where downloaded updates are kept can be shared with Android's installer. -->
<paths>
    <cache-path name="updates" path="updates/" />
</paths>
