# --- Signing keys and secrets: never commit these ---
*.jks
*.keystore
*.p12
*.pfx
keystore.properties
local.properties
.env
.env.*
*.pem
secrets.*
credentials.*
# Our own source file (the store for saved logins), not a secrets file:
!app/src/**/Credentials.kt
google-services.json

# --- Build output and tooling ---
build/
/build
/app/release
*.apk
*.aab
.gradle/
.kotlin/
.idea/
*.iml
.externalNativeBuild
.cxx
/captures

# --- OS and editor noise ---
.DS_Store
Thumbs.db
*.log
